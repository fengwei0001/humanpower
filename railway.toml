[build]
builder = "nixpacks"
watchPatterns = ["demo/**"]

[deploy]
startCommand = "serve dist -s -l ${PORT:-3000}"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"

[service]
rootDirectory = "demo"
